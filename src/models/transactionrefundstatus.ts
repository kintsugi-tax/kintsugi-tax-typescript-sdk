/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../types/enums.js";

/**
 * Shopify has 2 order statuses for refund case: refunded and partially_refunded
 *
 * @remarks
 * If the given order has different status from these 2, we will set the
 * transaction's refund_status to PARTIALLY_REFUNDED by default.
 */
export const TransactionRefundStatus = {
  FullyRefunded: "FULLY_REFUNDED",
  PartiallyRefunded: "PARTIALLY_REFUNDED",
} as const;
/**
 * Shopify has 2 order statuses for refund case: refunded and partially_refunded
 *
 * @remarks
 * If the given order has different status from these 2, we will set the
 * transaction's refund_status to PARTIALLY_REFUNDED by default.
 */
export type TransactionRefundStatus = ClosedEnum<
  typeof TransactionRefundStatus
>;

/** @internal */
export const TransactionRefundStatus$inboundSchema: z.ZodNativeEnum<
  typeof TransactionRefundStatus
> = z.nativeEnum(TransactionRefundStatus);

/** @internal */
export const TransactionRefundStatus$outboundSchema: z.ZodNativeEnum<
  typeof TransactionRefundStatus
> = TransactionRefundStatus$inboundSchema;

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace TransactionRefundStatus$ {
  /** @deprecated use `TransactionRefundStatus$inboundSchema` instead. */
  export const inboundSchema = TransactionRefundStatus$inboundSchema;
  /** @deprecated use `TransactionRefundStatus$outboundSchema` instead. */
  export const outboundSchema = TransactionRefundStatus$outboundSchema;
}
