/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exemptionsGetAttachments } from "../../funcs/exemptionsGetAttachments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetAttachmentsForExemptionV1ExemptionsExemptionIdAttachmentsGetRequest$inboundSchema,
};

export const tool$exemptionsGetAttachments: ToolDefinition<typeof args> = {
  name: "exemptions-get-attachments",
  description: `Get Attachments For Exemption

The Get Attachments for Exemption API retrieves all
    attachments associated with a specific exemption.
    This is used to view and manage supporting documents
    like exemption certificates uploaded for a particular exemption record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exemptionsGetAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
