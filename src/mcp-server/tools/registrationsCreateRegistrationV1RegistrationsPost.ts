/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { registrationsCreateRegistrationV1RegistrationsPost } from "../../funcs/registrationsCreateRegistrationV1RegistrationsPost.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRegistration$inboundSchema,
};

export const tool$registrationsCreateRegistrationV1RegistrationsPost:
  ToolDefinition<typeof args> = {
    name: "registrations-create-registration-v1-registrations-post",
    description: `Create Registration

The Create Registration API allows users to create a new registration
    for tracking and managing tax filings efficiently across multiple jurisdictions.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await registrationsCreateRegistrationV1RegistrationsPost(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
