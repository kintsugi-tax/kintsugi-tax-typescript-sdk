/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exemptionsGetExemptionByIdV1ExemptionsExemptionIdGet } from "../../funcs/exemptionsGetExemptionByIdV1ExemptionsExemptionIdGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetExemptionByIdV1ExemptionsExemptionIdGetRequest$inboundSchema,
};

export const tool$exemptionsGetExemptionByIdV1ExemptionsExemptionIdGet:
  ToolDefinition<typeof args> = {
    name: "exemptions-get-exemption-by-id-v1-exemptions-exemption-id-get",
    description: `Get Exemption By Id

The Get Exemption By ID API retrieves a specific exemption record by
    its unique ID. This API is useful for retrieving detailed information
    about a particular exemption, including its associated
    customer, organisation id, status, etc.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await exemptionsGetExemptionByIdV1ExemptionsExemptionIdGet(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
