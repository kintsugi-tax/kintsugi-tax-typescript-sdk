/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersGetCustomersV1 } from "../../funcs/customersGetCustomersV1.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCustomersV1Request$inboundSchema.optional(),
};

export const tool$customersGetCustomersV1: ToolDefinition<typeof args> = {
  name: "customers-get-customers-v1",
  description: `Get Customers

The Get Customers API retrieves
    a paginated list of customers based on specified filters.
    This API allows searching, filtering by country and state, and sorting the results.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersGetCustomersV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
