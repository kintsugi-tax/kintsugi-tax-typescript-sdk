/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filingsGetFilingByIdV1FilingsFilingIdGet } from "../../funcs/filingsGetFilingByIdV1FilingsFilingIdGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFilingByIdV1FilingsFilingIdGetRequest$inboundSchema,
};

export const tool$filingsGetFilingByIdV1FilingsFilingIdGet: ToolDefinition<
  typeof args
> = {
  name: "filings-get-filing-by-id-v1-filings-filing-id-get",
  description: `Get Filing By Id

This API retrieves detailed information about a specific
    filing using its unique identifier (filing_id).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filingsGetFilingByIdV1FilingsFilingIdGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
