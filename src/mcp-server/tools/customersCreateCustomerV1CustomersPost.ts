/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersCreateCustomerV1CustomersPost } from "../../funcs/customersCreateCustomerV1CustomersPost.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CustomerCreate$inboundSchema,
};

export const tool$customersCreateCustomerV1CustomersPost: ToolDefinition<
  typeof args
> = {
  name: "customers-create-customer-v1-customers-post",
  description: `Create Customer

The Create Customer API enables the creation of a new customer record with essential
details like name, contact information, and address, along with optional metadata.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersCreateCustomerV1CustomersPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
