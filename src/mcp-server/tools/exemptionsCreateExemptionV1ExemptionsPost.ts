/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exemptionsCreateExemptionV1ExemptionsPost } from "../../funcs/exemptionsCreateExemptionV1ExemptionsPost.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.ExemptionCreate$inboundSchema,
};

export const tool$exemptionsCreateExemptionV1ExemptionsPost: ToolDefinition<
  typeof args
> = {
  name: "exemptions-create-exemption-v1-exemptions-post",
  description: `Create Exemption

The Create Exemption API allows you to create a new exemption record.
    This includes defining details such as exemption type, jurisdiction,
    Country, State, validity dates, etc.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exemptionsCreateExemptionV1ExemptionsPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
