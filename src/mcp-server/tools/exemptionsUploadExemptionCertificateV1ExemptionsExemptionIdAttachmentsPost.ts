/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exemptionsUploadExemptionCertificateV1ExemptionsExemptionIdAttachmentsPost } from "../../funcs/exemptionsUploadExemptionCertificateV1ExemptionsExemptionIdAttachmentsPost.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .UploadExemptionCertificateV1ExemptionsExemptionIdAttachmentsPostRequest$inboundSchema,
};

export const tool$exemptionsUploadExemptionCertificateV1ExemptionsExemptionIdAttachmentsPost:
  ToolDefinition<typeof args> = {
    name:
      "exemptions-upload-exemption-certificate-v1-exemptions-exemption-id-attachments-post",
    description: `Upload Exemption Certificate

The Upload Exemption Certificate API allows you
    to upload a file attachment (e.g., exemption certificate) for a specific exemption.
    This is primarily used to associate supporting documents with an exemption record
    to ensure compliance and facilitate verification.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await exemptionsUploadExemptionCertificateV1ExemptionsExemptionIdAttachmentsPost(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
