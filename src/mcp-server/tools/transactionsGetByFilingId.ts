/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsGetByFilingId } from "../../funcs/transactionsGetByFilingId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetTransactionsByFilingIdV1TransactionsFilingsFilingIdGetRequest$inboundSchema,
};

export const tool$transactionsGetByFilingId: ToolDefinition<typeof args> = {
  name: "transactions-get-by-filing-id",
  description: `Get Transactions By Filing Id

Retrieve transactions by filing ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsGetByFilingId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
