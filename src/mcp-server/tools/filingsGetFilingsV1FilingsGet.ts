/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filingsGetFilingsV1FilingsGet } from "../../funcs/filingsGetFilingsV1FilingsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFilingsV1FilingsGetRequest$inboundSchema.optional(),
};

export const tool$filingsGetFilingsV1FilingsGet: ToolDefinition<typeof args> = {
  name: "filings-get-filings-v1-filings-get",
  description: `Get Filings

The Get Filings API retrieves a paginated list of filings based on
    filters such as dates, jurisdiction, Country, status, etc. This helps track
    and manage tax filings efficiently across multiple jurisdictions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filingsGetFilingsV1FilingsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
