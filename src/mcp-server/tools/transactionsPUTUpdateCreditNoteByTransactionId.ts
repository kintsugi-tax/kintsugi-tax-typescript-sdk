/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsPUTUpdateCreditNoteByTransactionId } from "../../funcs/transactionsPUTUpdateCreditNoteByTransactionId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PUTUpdateCreditNoteByTransactionIdRequest$inboundSchema,
};

export const tool$transactionsPUTUpdateCreditNoteByTransactionId:
  ToolDefinition<typeof args> = {
    name: "transactions-PUT-update-credit-note-by-transaction-id",
    description: `Update Credit Note By Transaction Id

Update an existing credit note for a specific transaction.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await transactionsPUTUpdateCreditNoteByTransactionId(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
